<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme overrides. -->
    <style name="Theme.Red" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Any attributes defined here will override those in the base themes.xml
             when the device is in dark mode.
             For example, if you wanted a different default background drawable in dark mode:
             <item name="android:windowBackground">@drawable/dark_mode_background</item>
        -->
    </style>
</resources>
